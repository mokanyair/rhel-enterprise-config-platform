---
- name: Manage approved sudo policy files
  ansible.builtin.copy:
    content: "{{ item.content }}"
    dest: "/etc/sudoers.d/{{ item.name }}"
    owner: root
    group: root
    mode: "0440"
    validate: "/usr/sbin/visudo -cf %s"
  loop: "{{ enterprise_sudo_rules }}"
