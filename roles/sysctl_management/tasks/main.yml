---
- name: Manage approved enterprise sysctl parameters
  ansible.posix.sysctl:
    name: "{{ item.name }}"
    value: "{{ item.value }}"
    state: present
    sysctl_file: /etc/sysctl.d/99-enterprise.conf
    reload: true
  loop: "{{ enterprise_sysctl_settings }}"
